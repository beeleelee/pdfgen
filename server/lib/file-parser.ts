// Module: server/lib/file-parser.ts — Placeholder for file content parsing.
// Reserved for future use when uploaded files need preprocessing
// (e.g., extracting structured data from .txt/.md files before feeding to the LLM).

export function parseFile(content: string, filename: string): string {
  return content
}
